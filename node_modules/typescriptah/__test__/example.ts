import { AfdelingOption, Client, nutriScoreOption, productFilter, sortByOption } from "../dist";

const client = new Client();

const filter: productFilter= {
    sortBy: sortByOption.nutriscore,
    property: [AfdelingOption.nonFood, AfdelingOption.diepvries],
}


/*
const filter: productFilter= {
    sortBy: sortByOption.nutriscore,
    property: [AfdelingOption.nonFood, AfdelingOption.diepvries]
}

console.log(product.getProductByName("test", filter));*/