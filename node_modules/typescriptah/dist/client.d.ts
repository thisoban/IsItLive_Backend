import { Product } from "./product";
export declare class AHShopClient {
    private readonly hostname;
    private readonly ahProduct;
    constructor();
    product(): Product;
    get(path: string): Promise<unknown>;
    request(path: string, method: requestMethod): Promise<unknown>;
}
export declare enum requestMethod {
    get = "GET"
}
